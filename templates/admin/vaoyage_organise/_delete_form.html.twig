<form method="post" action="{{ path('vaoyage_organise_delete', {'id': vaoyage_organise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vaoyage_organise.id) }}">
    <button class="btn">Delete</button>
</form>